{
  "name": "quantum-trading-bot-root",
  "version": "2.4.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "install": "npm --prefix frontend install"
  }
}
